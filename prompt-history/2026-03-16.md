# Prompt History - 2026-03-16

## Pre-Chat Project Context (Backfilled)

| Date & Time (Europe/Berlin) | Event |
| --- | --- |
| 2026-03-16 Pre-chat | Repo path set to `C:\dev\workflex` (after initial edits in a wrong path). |
| 2026-03-16 Pre-chat | Governance structure established: `agents/`, `rules/`, `doku/`, `requirements/`, `prompt-history/`. |
| 2026-03-16 Pre-chat | Requirements process defined: new/discussed scope -> `requirements/entries/` + `PFLICHTENHEFT`; completed scope -> `LASTENHEFT`. |
| 2026-03-16 Pre-chat | Pipelines split into `quality.yml`, `e2e.yml`, `build-release.yml`; old `ci.yml` removed. |
| 2026-03-16 Pre-chat | E2E baseline created under `e2e-tests/` (Playwright TypeScript; later migrated to Gherkin/Cucumber). |
| 2026-03-16 Pre-chat | CI permission incidents fixed (`backend/mvnw` executable, tracked `node_modules` removed, `npm ci` + `npx playwright`). |
| 2026-03-16 Pre-chat | Language policy enforced: repository content in English; chat can remain German. |
| 2026-03-16 Pre-chat | Backend error-response safety tightened to avoid leaking internals. |

## Current Session Timeline

| Date & Time (Europe/Berlin) | Event |
| --- | --- |
| ~21:14 | Rule overlap review started; Angular rule duplication identified. |
| ~21:15 | Gherkin support requested and implemented (Cucumber + Playwright). |
| ~21:16 | `E2E_GHERKIN_RULES.md` added with strict one-`Then` policy. |
| ~21:18 | Markdown lint spacing issue surfaced and fixed in governance docs. |
| ~21:22 | Frontend libs normalized so first-layer domains are inside each lib `src/`; empty dirs removed. |
| ~21:30 | Angular Material integrated (packages, global theme, async animations provider); frontend agent updated to prefer Material. |
| ~21:38 | VS Code integration request handled; OpenClaw TUI task added. |
| ~21:43 | VS Code run tasks added for backend/frontend and one-click combined startup. |
| ~21:48 | VS Code Postgres Docker Compose tasks added. |
| ~21:51 | Task labels shortened (`PG Up`, `Backend`, `Frontend`, `Start App`, `Start All`). |
| ~21:52 | Test tasks added: `FE Unit`, `BE Unit`, `E2E`. |
| ~21:54 | Prompt history backfilled with pre-chat project-start context. |
| ~21:57 | CI environment issue discussed; E2E pipeline self-provisioning path implemented. |
| ~22:02 | Pipeline DB connectivity failures traced and fixed in `quality.yml` + `e2e.yml`. |
| ~22:03-22:15 | VS Code backend task stability fixes: bash `./mvnw`, Java version guard, runtime `JAVA_HOME` derivation, Docker/DB readiness checks. |
| ~22:17 | Angular rules consolidated to canonical file (`rules/Angular/guidelines.md` + `llms-full.txt`, with README). |
| ~22:23 | E2E DB provisioning switched from Docker Compose to GitHub Actions `services.postgres`. |
| ~22:36-22:50 | Quality pipeline failures debugged and fixed (lint/prettier/test command + router test provider). |
| ~22:58 | Node 20 deprecation warning resolved by opting workflows into Node 24 JS actions runtime. |
| ~23:06-23:31 | Workflow framework created/refined: `DEVELOPMENT_WORKFLOW`, `DEFINITION_OF_DONE_WORKFLOW`, `DEFINITION_OF_READY_WORKFLOW`, `LESSONS_LEARNED_WORKFLOW`, and `lessons-learned/error-memory.md`. |
| 2026-03-16 23:34 | Prompt-history logging rule set: do not shorten prompt content; log prompts in full meaning, translated to English, with corrected spelling/grammar. |
