# Prompt History - 2026-03-16

## Pre-Chat Project Context (backfilled)

The project already had foundational governance and CI/CD work before this chat window started.
This section backfills that earlier context so the project history is complete.

- Repo path set to `C:\dev\workflex` (after initial edits in a wrong path)
- Governance structure established: `agents/`, `rules/`, `doku/`, `requirements/`, `prompt-history/`
- Requirements process defined:
  - add new/discussed scope to `requirements/entries/` + `requirements/PFLICHTENHEFT.md`
  - move completed scope to `requirements/LASTENHEFT.md`
- Pipeline split finalized:
  - `.github/workflows/quality.yml`
  - `.github/workflows/e2e.yml`
  - `.github/workflows/build-release.yml`
  - removed old `.github/workflows/ci.yml`
- E2E baseline created under `e2e-tests/` with Playwright TypeScript (later migrated to Gherkin/Cucumber)
- CI permission incidents fixed:
  - backend wrapper execute permission (`chmod +x backend/mvnw`)
  - tracked `e2e-tests/node_modules` removed; switched to `npm ci` + `npx playwright`
- Language policy enforced: repository content in English; chat language can stay German
- Backend error-response safety tightened to avoid leaking internals (stack traces, implementation details, DB internals)

## Current Session Timeline

Project setup, governance, CI/CD, and testing architecture were defined and iteratively refined.
Major outcomes include:

- agents/rules/doku/requirements structure
- separate quality, e2e, and build-release pipelines
- split unit-testing agents (frontend/backend)
- e2e strategy focused on completed feature money paths
- language policy set to English for repository content
- frontend agent updated to include Angular support docs from `rules/angular/`
- e2e migrated to Gherkin + Cucumber + Playwright support
- dedicated `E2E_GHERKIN_RULES.md` added with strict one-Then-per-scenario rule
- frontend libs normalized so first-layer domains are inside each library `src/` folder; empty directories removed
- Angular Material integrated (packages + global theme + async animations provider) and frontend agent updated to prefer Material components
- Added VS Code workspace task `.vscode/tasks.json` to run `openclaw tui` (default build task for quick in-editor chat workflow)
- Added VS Code run tasks for backend/frontend and a compound task to start both in parallel (`Workflex: Start Backend + Frontend`)
- Added VS Code Docker Compose tasks for Postgres (`up`, `down`, `logs`) and a full stack task (`Workflex: Start Postgres + Backend + Frontend`)
- Renamed VS Code tasks to short labels to reduce terminal/tab bar clutter (e.g., `PG Up`, `Backend`, `Frontend`, `Start App`, `Start All`)
- Added compact test tasks in VS Code: `FE Unit`, `BE Unit`, and `E2E`
- E2E GitHub Actions pipeline updated to self-provision Postgres via Docker Compose with readiness checks, failure log artifacts, and compose teardown
- Quality pipeline backend job updated to provide Postgres service (health check + explicit DB env) so Spring Boot tests no longer fail on `localhost:5433` connection refusal
