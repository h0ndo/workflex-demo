# Prompt History – 2026-03-16

## Verlauf (korrigiert und zusammengefasst)

### 10:54

**Nutzerwunsch:**
Ein Requirements-Engineering-Agent soll helfen, Anforderungen detailliert auszuarbeiten, auf blinde Flecken hinzuweisen und Ergebnisse in GitHub Stories/Tasks mit Fortschrittsstatus zu überführen.

**Umsetzung:**

- Agent-Spezifikation erstellt: `agents/requirements-engineering-agent.md`

### 10:59

**Nutzerwunsch:**
Zusätzlich ein dauerhaft gepflegter Ordner `prompt-history`, damit Gesprächskontext bei Problemen nachvollziehbar bleibt.
Außerdem soll Rechtschreibung in den Protokollen korrigiert werden.

**Umsetzung:**

- `prompt-history/README.md` erstellt
- Tagesprotokoll `prompt-history/2026-03-16.md` erstellt
- Korrigierte, lesbare Dokumentation als Standard festgelegt

### 11:03

**Nutzerwunsch:**
Alle Agent-Dateien sollen in den Ordner `agents/` verschoben werden.

**Umsetzung:**

- Vorhandene agent-bezogene Datei im Projektstamm verschoben:
  - `DB_MIGRATION_AGENT_RULES.md` -> `agents/db-migration-agent-rules.md`

### 11:06

**Nutzerwunsch:**
Die Rules sollen als Rahmenbedingungen für die Agents dienen.

**Umsetzung:**

- Regelwerke nach `agents/rules/` verschoben:
  - `BACKEND_CODING_RULES.md`
  - `BACKEND_STRUCTURAL_RULES.md`
  - `FRONTEND_CODING_RULES.md`
  - `FRONTEND_STRUCTURAL_RULES.md`
  - `API_CONTRACT_WORKFLOW.md`
  - `FLYWAY_WORKFLOW.md`
- Zusätzliche Agent-Dateien erstellt und an Rules gekoppelt:
  - `agents/backend-agent.md`
  - `agents/frontend-agent.md`
  - `agents/testing-agent.md`

### 11:23

**Nutzerwunsch:**
Ordner `doku` anlegen und die Definition als Datei `Definition_Agent_Roles` dokumentieren. Außerdem klare Trennung zwischen `agents/` und `rules/` herstellen.

**Umsetzung:**

- `doku/Definition_Agent_Roles.md` erstellt.
- Struktur finalisiert:
  - `agents/` für Rollen-Definitionen
  - `rules/` für generelle Regelwerke
- Rule-Dateien aus `agents/rules/` nach `rules/` verschoben und alten Unterordner entfernt.

### 11:32

**Nutzerwunsch:**
Ein Browser-naher UI/UX-Analyseprozess (Web/MCP-Kontext) soll als Workflow + Agent ergänzt werden.

**Umsetzung:**

- `doku/UI_UX_Analyse_Workflow.md` erstellt.
- `agents/ui-ux-agent.md` erstellt.
- Agent mit Frontend-Rules und Analyse-Workflow verknüpft.

### 11:35

**Nutzerwunsch:**
`API_CONTRACT_WORKFLOW` in Rules und Doku aufteilen.

**Umsetzung:**

- Neue Rule-Datei erstellt: `rules/API_CONTRACT_RULES.md`
- Neue Doku-Datei erstellt: `doku/API_CONTRACT_WORKFLOW.md`
- Alte Mischdatei entfernt: `rules/API_CONTRACT_WORKFLOW.md`

### 11:40

**Nutzerwunsch:**
Markdownlint-Fehler (`MD022`) projektweit beheben und Spellchecker auf Deutsch umstellen.

**Umsetzung:**

- MD022 (blanks around headings) in den relevanten Projektdateien automatisiert bereinigt.
- `.markdownlint-cli2.jsonc` angelegt, um Drittanbieter-Dateien auszuschließen:
  - `node_modules/`, `dist/`, `target/`
- `MD013` (Line Length) in der Markdownlint-Konfiguration deaktiviert, damit der Fokus auf relevanten Stilregeln bleibt.
- `.vscode/settings.json` angelegt und Spellchecker auf Deutsch+Englisch gesetzt (`de-DE`, `en`).

### 12:06

**Nutzerwunsch:**
Klären, ob `db-migration-agent-rules` im Ordner `agents/` richtig liegt oder getrennt werden sollte.

**Umsetzung:**

- Datei in den Rules-Bereich verschoben:
  - `agents/db-migration-agent-rules.md` -> `rules/DB_MIGRATION_RULES.md`
- Eigenen Agent ergänzt:
  - `agents/db-migration-agent.md`
- Backend-Agent auf die neue Struktur angepasst.

### 12:16

**Nutzerwunsch:**
Eine Projektstruktur-Rule erstellen und zusätzlich einen Ordner `requirements` einführen (Lastenheft/Pflichtenheft-Logik).

**Umsetzung:**

- Neue Strukturregel erstellt:
  - `rules/PROJECT_STRUCTURE_RULES.md`
- Neuen Bereich angelegt:
  - `requirements/README.md`
  - `requirements/LASTENHEFT.md`
  - `requirements/PFLICHTENHEFT.md`
  - `requirements/entries/TEMPLATE.md`
- Requirements-Agent erweitert, damit Ergebnisse künftig verbindlich in `requirements/` gepflegt werden.
- Veraltete DB-Regel-Referenz korrigiert:
  - `DB_MIGRATION_AGENT_RULES.md` -> `DB_MIGRATION_RULES.md`

### 12:21

**Nutzerhinweis:**
Requirements-Agent soll Lastenheft/Pflichtenheft verbindlich pflegen:
- bei neuer oder besprochener Aufgabe ins Pflichtenheft aufnehmen
- bei fertig bearbeiteter Story/Aufgabe Lastenheft aktualisieren

**Umsetzung:**

- `agents/requirements-engineering-agent.md` angepasst:
  - unpassende API-Contract-Rule entfernt
  - Pflegepflicht für `requirements/entries/`, `PFLICHTENHEFT.md` und `LASTENHEFT.md` explizit verschärft
  - Ablauf entsprechend konkretisiert

### 12:29

**Nutzerwunsch:**
Backend-Regeln für REST-Responses und Fehlerausgaben schärfen:
- keine Stacktraces/Interna nach außen
- klare Fehlerstatus (z. B. 404/500)
- bei Update das aktualisierte Element zurückgeben

**Umsetzung:**

- `rules/BACKEND_STRUCTURAL_RULES.md` erweitert:
  - API-Vertrag ergänzt: Update-/Patch-Operationen liefern das aktualisierte Element zurück.
  - Fehlerstandard verschärft: keine Klassen-/Methoden-/DB-Interna, keine Stacktraces,
    standardisierte Statuscodes und generische 500-Meldung.

### 13:14

**Nutzerwunsch:**
DevOps-Bereich ergänzen (Agent + Rules), Pipelines für Quality, Build/Release und E2E
aufsetzen sowie Root-Bereich `e2e-tests/` (Playwright + TypeScript) einführen.
E2E soll feature-basiert sein: Tests erst bei vollständig umgesetztem Feature,
orientiert an Lastenheft/Requirements.

**Umsetzung:**

- Neue Rules:
  - `rules/DEVOPS_RULES.md`
  - `rules/E2E_TESTING_RULES.md`
- Neue Agents:
  - `agents/devops-agent.md`
  - `agents/e2e-testing-agent.md`
- Projektstruktur-Rule erweitert (`.github/workflows/`, `e2e-tests/`).
- E2E-Basis eingerichtet:
  - `e2e-tests/package.json`
  - `e2e-tests/playwright.config.ts`
  - `e2e-tests/tests/smoke.spec.ts`
- GitHub Actions auf getrennte Pipelines umgestellt:
  - `.github/workflows/quality.yml`
  - `.github/workflows/e2e.yml`
  - `.github/workflows/build-release.yml`
  - alte `ci.yml` entfernt
- Pflichtenheft um DevOps-/E2E-Features ergänzt.

### 13:29

**Nutzerwunsch:**
Testing-Agent aufsplitten in Frontend-Unit-Testing und Backend-Unit-Testing.
Begründung: unterschiedliche Rules/Tools und getrennte Konfiguration.
Zusätzlich: Berechtigungen primär als Unit-Tests, E2E nur Money Path und
vollständige Features; generell so viel wie möglich in Unit-Tests auslagern,
um E2E-Menge, Laufzeit und Kosten zu reduzieren.

**Umsetzung:**

- Neue Unit-Testing-Rules erstellt:
  - `rules/UNIT_TESTING_FRONTEND_RULES.md`
  - `rules/UNIT_TESTING_BACKEND_RULES.md`
- Testing-Agent aufgesplittet:
  - `agents/unit-testing-frontend-agent.md`
  - `agents/unit-testing-backend-agent.md`
- `agents/testing-agent.md` auf "Deprecated" gesetzt mit Verweisen auf neue Agents.
- `rules/E2E_TESTING_RULES.md` geschärft:
  - Money-Path-Fokus
  - kein "klein-klein"
  - Berechtigungen/Konfigurationslogik primär als Unit-Tests
- `rules/PROJECT_STRUCTURE_RULES.md` um neue Agent-Aufteilung ergänzt.
- `requirements/PFLICHTENHEFT.md` um Eintrag zur Unit-Testing-Aufteilung erweitert.

## Offene Punkte

- Erste konkrete Anforderung für Workflex mit dem neuen Requirements-Engineering-Agenten strukturieren
- Daraus direkt GitHub Epic/Stories/Tasks ableiten
- Ersten realen Flow mit dem neuen UI/UX-Agenten durchlaufen und Findings als Issues erfassen

## Nächste sinnvolle Schritte

1. Ziel und Scope der nächsten Funktion in 5–10 Sätzen klären.
2. Anforderungen (funktional + nicht-funktional) strukturieren.
3. Akzeptanzkriterien formulieren.
4. GitHub Issues anlegen und priorisieren (MVP zuerst).
